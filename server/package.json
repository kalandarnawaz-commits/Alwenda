{
  "name": "alwenda-server",
  "version": "0.1.0",
  "private": true,
  "description": "Alwenda backend — the only place secret keys (OpenAI, Supabase service role) are allowed to live. Talks to the frontend over /api/*.",
  "type": "module",
  "scripts": {
    "dev": "node --env-file=.env --watch index.js",
    "start": "node --env-file=.env index.js"
  },
  "dependencies": {
    "@openai/agents": "^0.1.0",
    "@supabase/supabase-js": "^2.45.0",
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "zod": "^3.23.8"
  }
}
